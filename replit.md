# Paint Battle Website

## Running in Replit

The project is served from the `website/` directory by the `Start application`
workflow:

```bash
cd website && python3 -m http.server 5000
```

The workflow uses port 5000 so the site is available in the Replit web preview.
No package installation or environment secrets are required.